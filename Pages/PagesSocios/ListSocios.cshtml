@page
@model obligatorio.Pages.PagesSocios.ListSociosModel
@{
}

<div class="text-center">
    <h1 class="display-4">Lista de Prestamos</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Vehiculo</th>
                <th>Precio</th>
                <th>Cedula cliente</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in Model.prestamos)
            {
                <tr>
                    <td>@p.Fecha</td>
                    <td>@p.Vehiculo.ToString()</td>
                    <td>@p.Precio</td>
                    <td>@p.CedulaCliente</td>
                    <td>
                        <a class="btn btn-primary" asp-page="./UpdatePrestamo" asp-route-id="@p.Id">Editar</a>

                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>